{
  "name": "construction-pro-site",
  "scripts": {
    "build:css": "npx sass src/scss/main.scss src/assets/css/main.css --no-source-map --style=compressed",
    "serve": "npx @11ty/eleventy --serve"
  },
  "devDependencies": {
    "sass": "^1.89.1",
    "@11ty/eleventy": "^2.0.0"
  }
}
