---
import { NAV_LINKS } from '../../config/site';
import ThemeToggle from '../common/ThemeToggle.astro';

const currentPath = Astro.url.pathname || "/";

---
---
const currentPath = Astro.url.pathname || "/";
---
---
<nav class="bg-surface shadow-md py-4">
  <div class="container mx-auto flex justify-between items-center px-4">
    <a href="/" class="flex items-center space-x-2 group">
      <img src="/logo.svg" alt="Logo" class="w-10 h-10" />
      <span class="text-2xl font-bold text-headings group-hover:text-primary transition">WebsiteTest</span>
    </a>
    <ul class="flex space-x-6">
      {NAV_LINKS.map((link) => (
        <li class="relative group">
          <a
            href={link.href}
            class={`font-semibold px-2 py-1 rounded transition-colors duration-200 ${currentPath === link.href ? 'text-primary underline' : 'text-text hover:text-primary'}`}
            aria-current={currentPath === link.href ? 'page' : undefined}
          >
            {link.title}
          </a>
          {/* Dropdown placeholder for future use */}
          {/* <div class="absolute left-0 mt-2 w-40 bg-surface rounded shadow-lg hidden group-hover:block z-50">
            <a href="#" class="block px-4 py-2 text-text hover:bg-gray-100">Subitem</a>
          </div> */}
        </li>
      ))}
    </ul>
    <ThemeToggle />
  </div>
</nav>
