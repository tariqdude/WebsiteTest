---
// Enhanced Fallback Component for Failed Components
---

<div class="fallback-container p-6 m-4 border-2 border-gray-200 rounded-lg bg-gray-50 dark:bg-gray-800 dark:border-gray-700">
  <div class="flex items-center mb-4">
    <div class="text-gray-500 text-2xl mr-3">🔧</div>
    <h3 class="text-lg font-bold text-gray-800 dark:text-gray-200">
      Component Unavailable
    </h3>
  </div>
  
  <p class="text-gray-600 dark:text-gray-400 mb-4">
    This component is temporarily unavailable. This might be due to:
  </p>
  
  <ul class="text-sm text-gray-600 dark:text-gray-400 mb-4 space-y-1">
    <li>• Framework loading issues</li>
    <li>• Network connectivity problems</li>
    <li>• Component compilation errors</li>
    <li>• Browser compatibility issues</li>
  </ul>
  
  <div class="flex gap-3">
    <button 
      onclick="window.location.reload()"
      class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 transition-colors"
    >
      Reload Page
    </button>
    
    <button 
      onclick="history.back()"
      class="px-4 py-2 bg-gray-500 text-white rounded hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 transition-colors"
    >
      Go Back
    </button>
  </div>
  
  <details class="mt-4">
    <summary class="cursor-pointer text-gray-700 dark:text-gray-300 font-medium">
      Technical Details
    </summary>
    <div class="mt-2 p-3 bg-gray-100 dark:bg-gray-700 rounded border-l-4 border-gray-400">
      <p class="text-sm text-gray-700 dark:text-gray-300">
        <strong>Timestamp:</strong> <span id="error-timestamp"></span><br>
        <strong>User Agent:</strong> <span id="user-agent"></span><br>
        <strong>URL:</strong> <span id="current-url"></span>
      </p>
    </div>
  </details>
</div>

<script>
  // Populate technical details
  document.getElementById('error-timestamp').textContent = new Date().toISOString();
  document.getElementById('user-agent').textContent = navigator.userAgent;
  document.getElementById('current-url').textContent = window.location.href;
</script>

<style>
  .fallback-container {
    max-width: 600px;
    margin: 2rem auto;
  }
  
  .fallback-container button {
    transition: all 0.2s ease;
  }
  
  .fallback-container details summary:hover {
    color: #4F46E5;
  }
  
  @media (prefers-reduced-motion: reduce) {
    .fallback-container button {
      transition: none;
    }
  }
</style>
